# deploy — как мир ставится

Зона отвечает на один вопрос: **чем поднимается вход в поле**, и делает это так, чтобы подъём
не зависел ни от одного инструмента стройки (`kb:WORLD-54`: скоуп не привязан к стройке).

Мир поднимается **образом**. Образу нужен только докер: ни девконтейнера, ни ноды, ни чужой
оснастки. Консоль поверх этого появится отдельно — но мир, которому нужна нода, чтобы открыть
дверь, снова был бы привязан к чужому инструменту, поэтому первично здесь именно образ.

## Поднять

```sh
git clone <репозиторий> && cd world
./deploy/up.sh
```

Одна команда. Что она делает по шагам, и каждый шаг говорит о себе вслух:

1. проверяет, что на машине есть докер и compose v2;
2. создаёт общую сеть `omnifield-gateway`, если её ещё нет (идемпотентно);
3. проверяет, что виден склад пакетов пульта — **до** сборки, а не посреди неё;
4. собирает образ: фронт → бинарь → тонкий слой;
5. поднимает контейнер и **ждёт, пока дверь ответит** — «запущен» и «отвечает» разные вещи.

Порт наружу задаётся значением, а не зашит:

```sh
WORLD_PORT=8090 ./deploy/up.sh
```

### Что появится

| адрес | что там |
|---|---|
| `http://127.0.0.1:8080/` | **пульт мира** — кто сейчас в поле |
| `http://127.0.0.1:8080/healthz` | проба жизни: `{"service":"world","status":"ok"}` |
| `http://127.0.0.1:8080/api/locations` | реестр локаций ручкой |
| `http://127.0.0.1:8080/<имя-локации>/…` | маршрут в локацию, которая зарегистрировалась |

Плюс два именованных тома — `world-field` (реестр локаций) и `world-stand` (журнал прогонов
стенда). Это **состояние поля** (`kb:WORLD-28`), и оно переживает пересоздание контейнера.

Локации в поле нет ни одной, и это рабочее состояние, а не недоделка: дверь стартует пустой и
подхватывает локации по мере их регистрации (`kb:FUND-5`). Как локация попадает в поле —
`core/README.md`, ручка `POST /api/locations`.

### Снять

```sh
docker compose -f deploy/compose.yaml down      # контейнера нет, ПОЛЕ ОСТАЁТСЯ на томах
docker compose -f deploy/compose.yaml down -v   # вместе с полем: реестр локаций стирается
```

## Одна оговорка, которую надо знать заранее

**Поднимается мир одним докером. Собирается — внутри поля.**

Пакеты пульта (`@omnifield/probe-web-*`) лежат на складе локации `probe-web`, а он отвечает
только соседям по сети `omnifield-gateway` и наружу не торчит (`kb:FUND-5`). Значит на машине,
где поля ещё нет, шаг сборки пройти нечем — и `up.sh` говорит об этом прямо, а не роняет
`pnpm install` через две минуты чужим сообщением.

Выхода два:

- поднять `probe-web` в этой же сети — и собирать на месте;
- собрать образ там, где поле есть, и **перенести готовый**:
  ```sh
  docker save omnifield/world:dev | ssh <машина> docker load
  ./deploy/up.sh          # на той машине сборка уже не понадобится
  ```

Это ограничение **сборки**, а не подъёма, и лежит оно в зоне `web`, а не здесь: зона `deploy`
складывает то, что ей дали. Названо оно тут потому, что упрётся в него человек именно на
подъёме.

## Проверить, что поднялось

```sh
./deploy/probe.sh          # зелёный прогон: поднять и проверить
./deploy/probe.sh --red    # красный прогон: сломать нарочно и убедиться, что проба падает
./deploy/probe.sh --both   # оба подряд
```

Зелёный прогон проверяет пять вещей, и все — **снаружи контейнера, по опубликованному порту**:

| # | что | почему именно так |
|---|---|---|
| 1 | `/healthz` отвечает | дверь жива |
| 2 | пульт открывается | и это **сборка**, а не исходник: проба смотрит на ссылку `/assets/*.js`, спрашивает сам бандл и краснеет на `/src/main.tsx` |
| 3 | регистрация проходит | локация попадает в поле |
| 4 | маршрут работает | к локации ходят через дверь по её имени |
| 5 | **пересоздание** сохраняет поле | реестр лежит на томе, а не в слое контейнера |

Пятый пункт делает **пересоздание, а не рестарт**, и это главное место всей пробы: рестарт
сохраняет реестр и без тома — слой контейнера при рестарте никуда не девается. Проба на
рестарте зеленела бы на несмонтированном томе, то есть не была бы пробой (`kb:WORLD-32`).

**Красный прогон — не приложение к зелёному, а его подтверждение.** Он ломает подъём тремя
способами и требует, чтобы проба покраснела:

| поломка | что обязано покраснеть | что обязано остаться зелёным |
|---|---|---|
| том под реестр не смонтирован | «поле пережило пересоздание» | дверь, регистрация, маршрут |
| хост-порт занят | подъём падает **и называет порт** | — |
| пульта нет там, куда смотрит `WORLD_WEB_DIR` | «пульт открывается» | `/healthz`: несобранная витрина не повод не пускать в поле |

Пары в третьем столбце важны не меньше первого: проверка, которая краснеет от всего подряд,
причину не показывает и потому бесполезна.

Пробе нужен `curl` на машине. Самому миру для подъёма он не нужен — только докер.

## Не встало — куда смотреть

Первым делом — журнал: мир называет причину сам, первой же строкой. Отказ обязан называть
причину **и выход** (`kb:WORLD-31`) — если наткнулся на отказ без выхода, это дефект, а не
мелочь: заводи его сюда.

```sh
docker compose -f deploy/compose.yaml logs -f
docker ps -a --filter name=world-door        # встал ли контейнер вообще
docker volume ls --filter name=world-        # на месте ли тома
```

| симптом | причина | выход |
|---|---|---|
| `up.sh`: «докера нет» / «демон не отвечает» | нечем поднимать | поставить Docker Engine либо запустить демон |
| `up.sh`: «склада probe-web не видно» | сборка идёт вне поля | см. оговорку выше — перенести готовый образ |
| `up.sh`: «контейнер не встал на порт N» | хост-порт занят | `WORLD_PORT=8090 ./deploy/up.sh` |
| `up.sh`: «дверь не ответила за 30 секунд» | процесс поднялся, но упал или не слушает | журнал двери — там названа причина |
| страница отвечает `503 web-not-built` | пульта нет там, куда смотрит `WORLD_WEB_DIR` | пересобрать образ; дверь и ручки при этом работают |
| после пересоздания поле пустое | том не подключён | `docker volume ls` — тома `world-field` быть не должно только после `down -v` |

## Что здесь живёт

| файл | что это |
|---|---|
| `Dockerfile` | образ мира: пульт → бинарь → тонкий слой. Контекст сборки — **корень репозитория** |
| `Dockerfile.dockerignore` | что из корня попадает в контекст. Лежит рядом с `Dockerfile`, а не в корне: правило зоны живёт в зоне |
| `compose.yaml` | файл запуска: хост-порт, общая сеть, именованные тома, переменные среды |
| `up.sh` | подъём одной командой; каждый отказ называет причину и выход |
| `probe.sh` | проба подъёма — зелёный и красный прогоны |

### Раскладка образа

```
/app/world        бинарь двери (статический, без cgo)
/app/web/         СОБРАННЫЙ пульт — index.html и assets/
/field/           реестр локаций          ← именованный том world-field
/stand/           журнал прогонов стенда  ← именованный том world-stand
```

Переменные среды перечислены явно и в образе, и в файле запуска. Контракт — `core/README.md`:

| переменная | в образе | что задаёт |
|---|---|---|
| `WORLD_ADDR` | `:8080` | адрес прослушивания. **Крутить нечего**: на этот порт смотрят `EXPOSE` и `HEALTHCHECK`; наружу крутится хост-порт (`WORLD_PORT`) |
| `WORLD_WEB_DIR` | `/app/web` | каталог собранного пульта |
| `WORLD_DOOR_FILE` | `/field/locations.json` | реестр локаций |
| `WORLD_STAND_DIR` | `/stand` | каталог прогонов стенда |

Умолчания самого `core` относительные (`../web/dist`, `../field/locations.json`) и верны при
запуске из каталога `core/`. В образе таких каталогов нет — поэтому здесь всё названо явно, а
не оставлено на умолчания.

## Чего здесь НЕТ и не будет

- **девконтейнер.** Он остаётся инструментом разработчика — того, кто мир строит. Подъём мира
  от него не зависит, и проверяется это просто: **на машине без девконтейнеров дверь обязана
  встать**;
- **исходники двери и пульта** — они в зонах `core` и `web`. Здесь их только складывают, и в
  готовый образ уезжает только собранное. Проверить: `docker run --rm --entrypoint ls
  omnifield/world:dev -R /app`;
- **консоль мира** — это зона `cli`, отдельная вещь и отдельная задача;
- **публикация образа наружу.** Выпуск в общий магазин — отдельное решение; здесь только
  сборка и запуск.

## Проверка ради которой зона заведена

Чистая машина, только докер: одна команда — и вход в поле работает, пульт открывается,
локации регистрируются, а пересоздание контейнера поля не теряет.

module github.com/omnifield/world/control

go 1.26

// ЕДИНСТВЕННАЯ ЗАВИСИМОСТЬ ЗОНЫ, И ОНА ЗАВЕДЕНА ОСОЗНАННО (решение user, `WORLD2-141`).
//
// Ею контроллер один раз заходит на машину юзера ПАРОЛЕМ, чтобы положить туда ключ:
// системный ssh пароля неинтерактивно не берёт, подменять его обёрткой над `sshpass` —
// хак, писать свой транспорт вместо `docker context` — запрещено соседом. До этого зона
// жила на одной стандартной библиотеке; следующая зависимость — снова решение architect,
// а не привычка.
require golang.org/x/crypto v0.55.0

require golang.org/x/sys v0.47.0 // indirect

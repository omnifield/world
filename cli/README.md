# cli — консоль мира

Лицо мира для человека: `@omnifield/cli`. Поднимает вход, регистрирует локации, собирает их
из схем (`kb:WORLD-50` — сборка объединяет схемы и запускает их).

**Пока пусто: зона заведена, работа не начата.** Порядок выбран сознательно — сначала образ
(`deploy`), потому что образу для подъёма нужен только докер, а консоли ещё и нода на машине.
Мир, которому нужна нода, чтобы открыть дверь, снова привязан к чужому.

## Правило, с которым зона живёт

**Пульт и консоль — одно и то же двумя лицами** (`kb:WORLD-53`): всё, что можно из консоли,
можно из пульта; веб привилегий не получает и своей логики не заводит.
